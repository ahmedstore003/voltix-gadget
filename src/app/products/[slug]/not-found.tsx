import { ProductNotFoundContent } from '@/components/layout/ProductNotFoundContent';

export default function ProductNotFound() {
  return (
    <main className="flex-grow">
      <ProductNotFoundContent />
    </main>
  );
}
